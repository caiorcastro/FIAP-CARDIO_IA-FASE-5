# Copie este arquivo para `.env` e preencha as chaves.
# NUNCA versione `.env`.

# IBM Watson Assistant
WATSON_API_KEY=SUA_CHAVE_AQUI
# Dica: use o campo `url` das "Service Credentials" (normalmente termina com `/instances/<id>`).
# Exemplo: https://api.au-syd.assistant.watson.cloud.ibm.com/instances/<INSTANCE_ID>
WATSON_URL=SUA_URL_AQUI

# Opcional: link do console do IBM Cloud para o seu assistente (para facilitar o vídeo).
# Exemplo (varia por conta/região): https://cloud.ibm.com/services/conversation/...
WATSON_CONSOLE_URL=

# Watson Assistant V2 exige:
# - Assistant ID
# - Environment ID (ex: live)
#
# Se preferir, você pode preencher apenas `ASSISTANT_ID` e o backend usará o mesmo valor
# para assistant/environment (modo compatibilidade). O recomendado é usar as duas variáveis.
WATSON_ASSISTANT_ID=SEU_ASSISTANT_ID_AQUI
WATSON_ENVIRONMENT_ID=SEU_ENVIRONMENT_ID_AQUI

# Compatibilidade (opcional):
ASSISTANT_ID=

# Google Gemini
GEMINI_API_KEY=SUA_CHAVE_AQUI

# Execução do chatbot:
# - watson: usa IBM Watson (padrão)
# - local: modo offline para avaliação (sem credenciais)
CARDIOIA_ASSISTANT_MODE=watson

# (Opcional) Modelo do Gemini (varia por versão da API/conta).
# Se vazio, o backend tenta um conjunto de nomes comuns e faz fallback para extração local.
GEMINI_MODEL=

# (Opcional) Integração com fases anteriores como serviços externos:
# - Fase 3 (REST Alerts): exemplo http://127.0.0.1:8000/vitals
PHASE3_ALERTS_URL=
# - Fase 4 (CV): exemplo http://127.0.0.1:5001 (endpoints /health e /predict)
PHASE4_CV_URL=
