"""Backend package (Flask + integrações).

Este arquivo existe para permitir imports estáveis (ex: `from backend.app import create_app`)
quando o projeto é executado a partir da raiz do repositório.
"""

